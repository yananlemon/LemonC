class BoolTest10{
	void main(){
	    bool a;
	    int b;
	    a = true && testBool() || !(testBool());
        if( a ){
            b = 0;
            a = 1 > 0;
            if( a)
                b = getNum(10);
        }else{
            b = getNum(1);
        }
        printf("b=%d\n",b);// 10


	}

    // return false
	bool testBool(){
	    return false;
	}

	int getNum(int i){
	    return i;
	}
}