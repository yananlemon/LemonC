class BoolTest13{
	void main(){
        int a;
        bool b1;
        b1 = cal(1.0) && 1 > 0;
        if( b1 )
            a = 25;
        else
            a = 26;
        printf("a=%d\n",a);// a = 25
	}

    // true
	bool cal(float a){
	    float b;
	    b = 0.9 + 19.0;
	    return a > 0.0 && a < getFloat1() || a < b;
	}

	float getFloat1(){
	    return 0.9;
	}
}