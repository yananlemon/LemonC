# Archived Native Build Notes

The native backend Java prototype has been moved out of the source tree to:

```text
tools/native-experiment/reference/native-backend-java/
```

It is not currently wired into the Maven build and should be treated as reference material.

If this experiment is revived later, rebuild it deliberately in a separate module or copy it back
under a real source root with tests, dependency wiring, and output paths under `target/`.
